package ru.progwards.sid.Y02;

public interface SomeFunc <T> {
    T func(T t);
}
//Здесь T обозначает как возвращаемый тип, так и тип параметра метода func ().
//Это означает, что он совместим с любым лямбда-выражением, принимающим
//один параметр и возвращающим значение того же самого типа.
